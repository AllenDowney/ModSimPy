name: ModSimPy
channels:
  - conda-forge
dependencies:
  - python
  - jupyter
  - numpy
  - matplotlib
  - pandas
  - scipy
  - pint
  - sympy
  - lxml
  - html5lib
  - beautifulsoup4
  - pytables
  - pip

# pip install pytest nbmake
# pip install jupyter-book ghp-import
# pip install pypandoc fastdoc
